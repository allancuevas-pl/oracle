# Oracle — Current State (living handoff)

> **This is the only doc that changes often. Update it at the end of every working session** so the next agent — in any tool — can start cold. Keep it a snapshot of *now*, not a changelog: prune what's no longer true. For rules see [../CLAUDE.md](../CLAUDE.md), the map [ARCHITECTURE.md](ARCHITECTURE.md), the runbook [WORKFLOW.md](WORKFLOW.md).

**Last updated:** 2026-08-27 · branch `claude-code/main`

## Shipped & live

- **CRM Spine** — clients, briefs, properties, matches pipeline, activity feed, record workspaces. (Stage 1+.)
- **Comp database** — **110 comps** live on both deployments, all `source: property_lions`, all Industrial: 95 Brisbane-area leases Will supplied (`createdBy: import:will-leases-2026-07-28`) + 15 seeded Thomastown/Blaxland demo comps. This is the **deliberate, curated** state — not a fault. Browse/search + Comp Scanner + Quick Add all work. Paginated browse + address search index. Importer: `AG-ORACLE/comp-importer/` (Python). See "comp archive history" below before adding bulk data.
- **IM scanner** — upload IM PDF → Claude extraction → create property. Browser-side photo extraction from the IM (pdf.js) with hard-won reliability fixes.
- **Property photos** — extract-from-IM, plus manual add / delete, hero+thumbnail gallery, keyboard lightbox, "Pull from IM" backfill. (2026-06-16)
- **Property videos** — Videos tab: hosted links (YouTube/Vimeo/Loom) + uploads; surfaced to clients as a "Walkthrough" in the deal portal. (2026-07-06)
- **Client portal** — token-gated deal reports; clients see only their shared deals and can approve/decline.
- **Client portal — branded `/portal` login** — ceremonial navy/gold single-column (Marcellus + Jost), `src/pages/client/ClientPortalLogin.jsx`: **email+password, Google, forgot-password reset, AND first-time invitation accept all functional** on the prod Clerk instance. The invite-accept flow ('accept' mode) consumes Clerk's `__clerk_ticket` via `useSignUp()` and shows a branded "Set Your Password" step — without it invited clients hit "can't find account" (fixed 2026-07-23). Signed-out `/client/*` redirects to `/portal`. (2026-07-08; unblocked by the prod-Clerk cutover 2026-07-23.)
- **Client portal — AML compliance documents** — staff upload/list/delete per client (`components/clients/ClientDocuments.jsx`); client sees a read-only "Compliance documents" section (`ClientDashboard`). Backend: `clientDocuments` table + `convex/clientDocuments.ts`; client read `clientPortal.getMyDocuments` (email-scoped). Portal-invite management (invite / revoke & resend / revoke access) in `components/clients/ClientPortalAccess.jsx`. (2026-07-08) **Enhanced 2026-07-23:** the panel now shows the TRUE Clerk invitation status (Not invited / Invited·pending / Accepted / Active / Revoked / Expired) with dates, via new `team.getPortalInviteDetail` action (reads Clerk's REST invitations API; validated against live data). Local-table status was misleading (pending row deleted on sign-in + seeded during cutover); Clerk is now the source of truth, with graceful fallback to local if the fetch fails.
- **Feaso & Reports** — feasibility model + branded report send-to-client flow.
- **🚀 PRODUCTION LAUNCH — live on `oracle.propertylions.com.au` behind Clerk PRODUCTION (2026-07-23).** One host: staff CRM at the root, client portal at `/portal`. DNS on **GoDaddy** (6 CNAMEs, verified). Own Google OAuth, password + email-code, waitlist off, `convex` JWT template present. Invite URLs + JWT issuer are now env-driven (`convex/team.ts` `inviteRedirectUrl()`, `convex/auth.config.ts`). The empty-prod-Clerk-DB lockout was prevented by `convex/migrations.ts` `seedInvitationsForClerkCutover` (ran: 4 users seeded). **Verified end-to-end incl. a real admin sign-in.** Full runbook, env inventory, rollback, and deploy gotchas: **[CLERK_PRODUCTION.md](CLERK_PRODUCTION.md)**.
- **Favicon** — Property Lions gold lion crest on navy (was the scaffolded Convex logo). `public/favicon.ico` + `favicon-16/32/192.png` + `apple-touch-icon.png`, generated from `public/property-lions-logo.png` (crest only). (2026-07-23)

## In flight / next up

- **Directory tab — BUILT + DEPLOYED live (2026-08-21).** Will's Jul-31 request: one searchable place for clients + agents + contractors/inspectors + solicitors + brokers, filter by category + state. New `contacts` table + `convex/contacts.ts` (CRUD, indexed); `src/pages/Directory.jsx` (aggregates live clients + the new contacts store) + `components/directory/ContactModal.jsx`; `/directory` route + Sidebar item. Live on both Convex deployments + both Vercel domains (deployment `oracle-ntwf8e5o6`). `/directory` serves 200 on the custom domain. **Not yet visually verified behind CRM auth** (build/lint/authz-gate/query-guard all green; app boots clean).
- **Comp Scanner — BUILT + DEPLOYED live (2026-08-21).** Will's Aug-21 request ("comps scanner like the IM scanner"). "Scan Comps" on the Comps page: upload an agent's comp table (PDF/screenshot) or paste text → `compExtractionAction.extractComps` (Claude, Node action) parses to our comps shape → review table → bulk import via `comps.createComps`. New `comp_scan` source. Verified end-to-end vs the live model (mixed sale+lease table extracted cleanly). `components/comps/CompScannerModal.jsx`.
- **Deal Vault — BUILT + DEPLOYED live (2026-08-21).** Will's Jul-3 + Aug-21 ask ('restricting access to the deal vault'). Property 'Files' tab is now a Deal Vault: upload sensitive deal docs with per-file access control. New `dealFiles` table + `convex/dealFiles.ts` (staff CRUD, signed URLs); `visibility` = internal (staff-only) | client (portal-shareable); optional category. `FilesTab.jsx` rewritten (dropzone, visibility toggle, category, delete); new files default Internal. **Client surfacing COMPLETE (2026-09-02)** — see below. On both deployments + both domains.
- **FEASO Google Sheet export — LIVE, now CLONES the real PL template (2026-08-26).** Will's Aug-21 ask. `convex/googleSheets.ts` `generateFeasoSheet`: service-account JWT → **copies the master PL FEASO template** (a Google Sheet in the `ORACLE FEASO` Shared Drive) via the Drive API, then fills the cells Oracle knows — report date, subject-property summary row, and linked leasing + sales comparable evidence — leaving every PL formula, cross-sheet reference and number format intact. Output = a real 4-tab PL FEASO (Property Assessment / Project Feasibility / Cashflow Inputs & Analysis / Outgoings), ~60% pre-filled, analyst finishes the judgment cells. **Verified end-to-end** (subject row incl. Rent/sqm formula preserved; comps render with $/sqm, yield, dd/mm/yyyy dates, currency — all via the template's own formats). Env: `GOOGLE_SA_CLIENT_EMAIL` + `GOOGLE_SA_PRIVATE_KEY` + `GOOGLE_SHARED_DRIVE_ID` + **`GOOGLE_FEASO_TEMPLATE_ID`** (`1o-q-vucFVKL0S5byTnPX9fU9FEoiKU1SdS5TIi6VXPI`) on both deployments. **To change FEASO layout/formulas → edit that master sheet, not code.** The prior raw 4-tab generator was replaced. Runbook: **[GOOGLE_SHEETS_SETUP.md](GOOGLE_SHEETS_SETUP.md)**. Dep: google-auth-library. Test artifacts trashed 2026-08-26.
- **FEASO workflow — embed + suggested comps (2026-08-26).** (a) The FEASO Sheet sub-tab now **embeds the generated sheet inline** (iframe /preview) with Open-in-Google-Sheets + Regenerate — "a copy in Oracle", still linked to Drive. (b) **Suggested comps**: on Feaso → Property Assessment, an **"Add comps"** button opens a panel that matches unlinked comps **suburb-first then widened to nearby suburbs** (same state + asset type; new `by_state_assetType_type` index) via `comps.suggestCompsForProperty`, badging non-suburb matches as 'nearby', and one-click links them → they flow into the evidence tables and the generated sheet. The missing link between a property and a populated FEASO. `AddCompsModal.jsx`.
- **Comp archive history — read before bulk-loading comps (corrected 2026-08-27).** The docs previously claimed ~262k comps were live, then claimed the import had vanished for "unknown" reasons. **Both were wrong.** What actually happened, per commit `3009eb1`: on **6 Jul** ~262k comps were loaded (257,815 Arealytics + 4,531 from Property Lions' own per-state workbooks). On **28 Jul** that archive was **deliberately wiped** — Will reported the data was largely inaccurate — and replaced with 95 curated Brisbane industrial lease comps he supplied. `comps.purgeImportedComps` (internal, batched, source-filtered) is the tool that did it. STATE.md was simply never updated on 28 Jul, so the stale "~262k loaded" line survived until 26 Aug.
  - **Do NOT reload Arealytics (257,815 rows).** That is the vendor data Will pulled for accuracy. He was re-evaluating the vendor as of 31 Jul ("claims to have improved their data issues") — his call, not ours.
  - **The 4,531 curated rows are a different question.** They come from PL's *own* per-state workbooks ("NSW Comps", "QLD Comps", …), clean `Leased`/`Sold` tabs, with the Arealytics/Dump sheets deliberately skipped by the parser. They cover **QLD 3,488 · VIC 620 · NSW 324 · WA 90** — well beyond the Brisbane-only live set. They appear to have been collateral damage in the 28 Jul sweep rather than a separate rejection, **but this is unconfirmed — ask Will before reloading.**
  - **What they'd fix:** of the 14 properties in Oracle, 12 have a suburb and **7 currently have zero comps** while the curated set has coverage — Burleigh Heads 134, Sumner 51, Epping 42, Ashmore 22, Byron Bay 22, Tuggerah 8, Rutherford 8. Suggested comps would go from working on ~3 properties to ~11.
  - **Reversible:** the payloads are still on disk (`comp-importer/out/comps_import.jsonl` 4,531 · `arealytics_import.jsonl` 257,815) and land under distinct `source` values, so a load can be purged again cleanly by source.
- **Will testing call 2026-09-02 — 6 of ~11 items done.** Full triage in `AG-ORACLE/BACKLOG.md`. Shipped:
  - **"Add Property" is now findable.** ⚠️ *The feature was never missing.* `MatchPropertyModal` has always added any property to a brief regardless of criteria — but the only entry point was a 32×32 **icon-only** button among identical icons, and the empty state said "use the link icon in the toolbar". Will couldn't find it and **neither could Allan, live, in his own app**; both concluded it was unbuilt. Fixed with a labelled `+ Add Property` in the empty state and in the pipeline header. **No backend change — none was needed.** Lesson: an icon-only primary action with a tooltip label is invisible under pressure.
  - **"Land" added as an asset type** (the call's one aligned decision). The list had drifted across **five** places — `settings.ts` seeded 3, comp form + comp filters hardcoded 6, extraction prompt named 5 — so a comp tagged "Mixed Use" could never match the filter. All now import **`convex/assetTypes.ts`** (canonical). `settings.assetTypes` is a stored admin-editable row, so `migrations:syncAssetTypes` (additive, idempotent, preserves custom types) was run: dev had the original 3 → now 7; the prod deployment has no settings row and serves the seed default.
  - **Comp scanner now takes Excel + drag-and-drop.** The model API doesn't accept spreadsheets as documents, so a workbook is read in the browser and flattened to a labelled TSV sent down the existing pasted-text path — no backend change. **Every sheet is kept and prefixed with its tab name**, because Will's workbooks split leasing/sales across tabs and the tab name is often the only type signal. This also fixes his state/land-size complaint: both survive the spreadsheet path where the screenshot route dropped them. Reader is **`read-excel-file`**, lazy-imported (52 KB raw / 16 KB gz, its own chunk — initial load unchanged at 107 KB gz). ⚠️ **Deliberately NOT `xlsx`**: npm's latest SheetJS (0.18.5) carries two unfixed advisories (prototype pollution + ReDoS) because patched builds ship only from SheetJS's own CDN.
  - **Suburb now shows on comp rows.** It previously appeared *only inside* the amber badge as "Suburb · nearby", so a same-suburb comp showed no location at all. Address lines in `AddCompsModal` and both Feaso evidence tables now carry suburb + state + postcode, with the badge reduced to just "nearby".
  - **Brief target duration "not specified" — FIXED, and nothing was ever lost.** The live `Alan Parsons` brief (created during that call) holds `durationMin: 0, durationMax: 6`. The display guarded on `brief.durationMin && brief.durationMax`, so a **minimum of 0 is falsy** and the whole range rendered "Not specified" — and the slider's own minimum is 0, so it was reachable just by dragging the handle left. Four such truthiness checks fixed (duration on BriefView; budget on BriefView, ClientView, Briefs). No migration needed. Also stopped `BriefModal` manufacturing the same state: it loaded with `editingBrief.durationMin || 0`, so opening a brief with no duration set the slider to 0/0 and saving persisted 0/0. **Watch for this pattern elsewhere — `value && other` on any numeric field treats a legitimate 0 as missing.**
  - **Scan review table now shows land size + date — the data was never missing.** Will: "Oracle looks like it's not picking up the land size... maybe it does, but in the scan summary it doesn't show it." It does: the prompt asks for `landAreaSqm`, the parser stores it, the import writes it. The review step — the one place for checking a scan before committing — simply had no Land column. Added Land + Date, relabelled NLA → "NLA / Build" (it's building size for a sale), review step widens to `max-w-6xl` with the table scrolling in its own container. *(His "no state" from the same call was different: that column already existed and his source screenshot genuinely had none — the Excel path fixes that.)*
  - **Still open from that call:** · adopted cap-rate % and asking-price editing in Feaso (**both need reproduction — the code already renders `%` and the field is editable, so this may be missing `marketRentLow`**) · Google Sheets iframe sign-in loop on Will's PC (works for Allan; needs his browser) · sales comps needing a hard refresh · regenerate-vs-resync semantics · scanner speed (~30s for 25–30 comps).
  - **Will confirmed the comps search fix works** on the call: "before you could only type an address, but now you can type in like Vic or Thomastown — that's working now."
  - **Comps now 103 lease + 39 sales.** Will scanned some in live and has an assistant lined up to audit them. He also said "we got heaps more, we'll load them once the system's up and running" — relevant to the parked 4,531 question: he intends to supply curated data himself.
- **Client-visible deal files + portal access hardening — DONE + LIVE (2026-09-02).** Completes the Deal Vault: `visibility: "client"` had been settable since 25 Aug but `clientPortal.ts` never referenced `dealFiles`, so staff could flag a document for a client who would never see it. `getMyReport` now returns client-visible files with signed URLs and `ClientDealView` renders a **Documents** section; internal files are filtered **server-side** and never leave the backend, not even as metadata.
  - **⚠️ Fixed a live access hole in the same pass.** `getMyReport`'s email check ran only `if (brief?.clientId)`, so a brief without one skipped verification entirely and any `client`-role user holding the token got the whole report. **4 of the 8 live briefs have no `clientId`** — this was real, not theoretical, and the payload now carries loan papers and legal records. It now **fails closed**: missing brief, missing clientId, missing identity email, or a client-email mismatch all return null.
  - Tests **14 → 23**. New `convex/clientPortal.test.ts` covers the deny paths first (wrong client · no client link · staff user · unauthenticated · unknown token) plus that an internal filename appears nowhere in the payload. Files are seeded with real stored blobs so `getUrl` resolves — an unstored id returns null and would make the assertion vacuous.
- **Custom dropdowns — DONE + LIVE (2026-09-02).** Will's 21-Aug white-on-white-on-PC bug. Every native `<select>` in `src/` is gone (10 across Directory, ContactModal, Comps, QuickAddCompModal, FilesTab) — `grep -rn '<select' src/` returns nothing. The two in `ContactModal` are react-hook-form fields and use `Controller`, since `register` cannot wire a Headless UI Listbox. **Also closed the `CustomSelect` a11y quick win in the same pass:** a Listbox button is not a `<select>`, so the visible `<label htmlFor>` never named it — 15 call sites announced as unlabelled buttons. `CustomSelect` now accepts `id` / `ariaLabel` / `ariaLabelledBy` and has a visible focus ring; all **26** usages app-wide pass an accessible name (there is a static check for this in the commit). **Not visually verified** — the CRM sits behind Clerk auth and no credentials were used; `CustomSelect` was already in production use in 8 other files, and build/lint/contract-check are green.
- **Will's Loom (2026-08-27) — 5 of 6 done, 1 open.** Done + live: dead header buttons fixed (the whole header was scaffolding — search box, bell dot and the "+ New Brief" CTA all had no handlers); comps search now matches suburb/state/postcode not just address (new `searchText` blob + `search_text` index + `migrations:backfillCompSearchText`, run on both deployments); "Other" free-text on brief asset types + strategies (`TagPicker allowOther`); last-modified line on a comp; scan-mode toggle (IM vs comp table) on the Oracle Scanner. **Still open: "search a suburb and get surrounding suburbs too."** Blocked on geography — `postcode` is 0% populated on all 110 comps and there is no lat/lng, so there is nothing to compute proximity from. Same-state is far too broad to call 'nearby' (North Lakes would return the whole of QLD). Real options: geocode suburbs via the Google Maps key already used for address autocomplete and store lat/lng, or backfill postcodes and use numeric adjacency. Needs a decision before building. No-action items from the same Loom: deal structure ("we don't need that specifically") and the property activity log (he found it already built).
- **Meeting backlog — superseded by `AG-ORACLE/BACKLOG.md` (rebuilt 2026-09-02); read that first.** Note the ordering change: **DD task management is Will's stated #1 but Allan has moved it LAST (2026-09-02)** as the largest build, with everything smaller and independent going first. Historical detail below. (1) **DD task-management / pipeline checklist** — task assignment, client-visible progress, contract important-dates, email reminders (Will's Jul-31 #1; he saw it in Allan's *other* system, not Oracle). (2) ~~Directory~~ ✅ built above. ~~(3) Building-grade field~~ ✅ (Prime/A/B/C on comps — form + list column, 2026-08-21). ~~(4) Comps summary State column~~ ✅ (added 2026-08-21). (5) **Feaso Cashflow (10-yr model)** — the Cashflow sub-tab is a placeholder stub. (6) **FEASO next**: per Aug-21 Will wants FEASO → an editable **Google Sheet** export (AI pre-fills comps/tenancy/cashflow), NOT a full in-app 10-yr calculator. Also new from Aug-21: **deal-vault access restrictions**, **email automations**, **lease-stock calculator** (property tab, low priority, pending Nick), **custom dropdown component** (native selects show white-on-white on PC — affects new Grade/State/Category selects), and ~10 minor updates pending a Loom from Will (client-profile fields, "Other" property type in briefs). Deferred/agreed-non-essential: **map view for comps**, **Aerolytics API** (Will to re-evaluate vendor first). Provenance: `AG-ORACLE/Google Meet Notes/` (Jun-26, Jul-03, Jul-31 PDFs).
- **Feaso Cashflow tab** — only the **Cashflow** sub-tab is a stub ("Coming in the next phase"); Property Assessment + Project Feasibility are already built. (Property tabs wired inline in `src/pages/PropertyView.jsx`; see ARCHITECTURE.md "Common change: add a property-detail tab".)
- **Invite a real client through the live portal** — the plumbing is verified end-to-end, but no genuine external client has been onboarded on prod yet. Good first confidence check next session.
- **Arealytics comp import polish** — loaded, but lease $/sqm not computed (source building-size ≠ leased area); revisit if needed.
- **Quick wins:** staff portal invites; lease-expiry column in the property evidence table; `CustomSelect` `aria-labelledby` a11y; video count/thumbnail on the property list rows.

- **Adopted cap rate — decided + built (2026-09-04).** Allan's call: *"cap rate would be added manually by the team in most cases. Oracle could suggest one if it has cap rates from comps."* So the FEASO sheet's `I25`/`I26` cap-rate scenarios stay **manual and untouched by the generator** — that question is now closed. Oracle instead shows a suggestion beside the Adopted cap rate field on Feaso → Project Feasibility: median of the linked sales comps that carry a cap rate, with the sample size and range, and a "Use x%" link. **Nothing is auto-applied.** Median not mean (small samples; one distressed sale would drag an average). `src/utils/capRate.js`, 9 tests.
  - **⚠️ It will be blank on real properties today, and that's a data gap not a bug.** Of 39 sale comps: **39 have a price, 6 have a cap rate, 5 have rent** — and **neither property with a feaso has a single linked sales comp carrying a cap rate**. You cannot derive a yield without income. Worth raising with Will alongside the comp audit his assistant is doing: sales evidence needs a yield or a rent column to be useful here.

- **FEASO generation tested live in the UI (2026-09-04) — verified, and it surfaced two more bugs.**
  - **Verified on 15 Boron Street (0 tenants)** — the hardest case, since that's where the template's stale tenancy block would show through. Result: rent `150/200`, land `8000/10000`, build `8000/12000`, offer `$2.5M`, project time `2 years`, and **rows 14–18 completely cleared** — "AH Beard" and all the previous deal's figures gone. Also verified on 44-56 Potassium Street (1 tenant), where the real tenant is written instead.
  - **⚠️ Google rate limit had NO retry — FIXED.** Regenerating twice within a couple of minutes tripped Drive's per-user limit; the generation failed outright and rendered a **raw Convex stack trace** in the property view. A manual retry a minute later worked. Added `fetchWithRetry` (1s/2s/4s) around Drive `files.copy` and Sheets `batchUpdate`, plus human-readable errors. **Drive returns 403 for both "not permitted" and "too fast"**, separated only by `reason` — `isRetryableGoogleError` is exported and tested both ways so a permissions 403 is never retried.
  - **⚠️ The embedded sheet shows Google's raw "You need access" page** when the signed-in Google account isn't a member of the `ORACLE FEASO` Shared Drive. No explanation from Oracle — just Google's request-access screen inside the iframe. **This is very likely what Will hit on 2026-09-02** (he described a persistent sign-in popup); it was diagnosed then as a caching bug, but it is a **Drive membership** problem. Fix: detect it and show "this sheet lives in the ORACLE FEASO Shared Drive — ask an admin for access", and/or add the team to that drive. **Not yet done.**
  - **Housekeeping:** two verification sheets are in the Shared Drive and the service account **cannot delete them** (it can create but not remove). Trash manually: `FEASO — 44-56 Potassium Street, Narangba` (`1EHT1hdm...`) and the regenerated `FEASO — 15 Boron Street, Sumner` (`1WREzezg...`, now the property's live sheet — keep this one, it's current).

- **⚠️ FEASO sheet was shipping a PREVIOUS DEAL's numbers — FIXED 2026-09-04.** `generateFeasoSheet` wrote **only** the Property Assessment tab and inherited tabs 2-4 from the master template untouched, on the assumption they were formula-driven off page 1. **They are not:** the master holds **42 numeric literals on Project Feasibility** and 44 on Cashflow, frozen from whatever deal it was last saved from — and the tenancy block carried a **real tenant name from another client's deal** ("AH Beard"). Every FEASO generated before this date shipped with those figures. Will spotted it on 2026-09-02 ("that's from a previous campaign"); it was recorded in the first triage and then **dropped from the working list** — a process failure worth not repeating.
  - The `feasos` record was never even fetched. It is now, and Oracle's fields are written to the **Project Feasibility** tab: `B3/C3` market rent, `B4/C4` LAND $/sqm, `B5/C5` BUILD $/sqm, `C8` offer price (B8 stays a formula off page 1), `C25` project years, `A14:H18` tenancy from `properties.tenants`, `K/M` per-tenancy market $/sqm from the adopted range. Unset fields write `""` — an empty cell is honest, a stale one isn't. Columns I/J/L are template formulas and are left alone.
  - **⚠️ Row 4 is LAND and row 5 is BUILD** — the reverse of the order in our schema. The map was read off the template with **`googleSheets:inspectFeasoTemplate`** (kept, internal, read-only — re-run it whenever the master layout changes) rather than guessed.
  - **Still stale, deliberately:** `I25`/`I26` hold two cap-rate scenarios (5.25% / 5.50%) and Oracle has a single `adoptedCapRate`. **Needs Will to say which maps where.** The **Cashflow Inputs & Analysis** tab (44 literals) and **Outgoings** (4) are also still untouched — same class of problem, not yet mapped.
  - **Verified end-to-end** on 44-56 Potassium Street: rents 115/140 → 200/250, offer $6.4M → $15M, tenant "AH Beard" → "TRI Underground Australia", size 3880 → 9540, expiry 2025 → 2028, rows 15-18 cleared.
  - **Housekeeping:** a verification sheet was generated in the Shared Drive and **could not be deleted by the service account** (it can create but not remove). **Trash `FEASO — 44-56 Potassium Street, Narangba` (id `1EHT1hdm...`) manually**, as was done on 2026-08-26.

- **UI smoke test run live in the CRM (2026-09-04) — 14 of 14 passed.** *(second batch below the first)* Verified in the browser on production, not just in the bundle: comps filters (State=VIC → exactly 15), comps suburb search ("North Lakes" → 7, was 0), custom dropdown (dark panel + gold tick, `aria-label="Comp source"`), **Land** present in the asset-type filter, `+ Add Property` button on a brief, Alan Parsons **Target Duration "0 to 6 years"** (Will's exact reported bug), suburb + state on comp rows in the Feaso evidence table and the suggested-comps panel (with `nearby` as a separate badge), and an **Excel upload scanned end-to-end** — a 2-sheet workbook returned all 4 comps with **state and land size populated**, which were Will's two scanner complaints. Nothing was imported; no test data written (comps still 142).
  - **Second batch (read-only, all passed):** comp editor shows **"Added 27 Aug 26 · never edited"**; **Land** present in the comp form's asset dropdown *and* the brief form's Asset Types (not just the filter); **"+ Other"** free text works interactively — typing "Childcare" + Enter produced a removable gold pill; the header **"+ New Brief"** CTA navigates from `/comps` to `/briefs` **and opens the modal**; the brief **empty state** shows the labelled `+ Add Property` with "doesn't have to match the brief's criteria"; Directory **state filter** and the Add Contact **Category**/**State** dropdowns (the `Controller`-wired pair, the riskiest swap) all render as custom panels with correct defaults and accessible names. New-brief Target Duration defaults to "1 to 3 years" — the `DEFAULT_DURATION` constant.
  - **No writes:** briefs 9, comps 142, contacts 0, matches 4 — unchanged. The brief and contact modals were cancelled.
  - **Access note:** the CRM is Clerk-gated and the browser tooling could only reach a profile signed in as `allan@foundationproperty.au`, which is role `client`. Tested by temporarily flipping that account to `admin` via `migrations:setRoleByEmail`, then reverting. Both rows are back to `client` — verified.
  - **New minor findings:** (1) an unknown path such as `/scanner` renders a **blank screen** — the catch-all route falls through to the CRM shell with no matching child and no 404. The real scanner route is `/oracle`. (2) `allan@foundationproperty.au` now has **two `users` rows** (a Google sign-in created a second Clerk identity), joining the two 27-Jul cutover duplicates — a cleanup migration is worth doing. (3) The junk date `"Upon Completion"` is visible in the suggested-comps panel, confirming the audit note.

## Known gotchas / tech debt (don't regress)

- **Deploy aliases BOTH domains.** `vercel --prod` does NOT auto-move the custom domain `oracle.propertylions.com.au` — alias both it AND `oracle-psi-beryl.vercel.app` to the new deployment every time, then verify a deep link (`/portal`) returns 200 on the custom domain, not just `/`. (WORKFLOW.md step 4.)
- **`CLERK_JWT_ISSUER_DOMAIN` must stay set on both Convex deployments.** `auth.config.ts` references it, and Convex statically requires any referenced env var to be *set* at deploy time — the `|| fallback` does NOT satisfy the check, so `convex dev --once` / `convex deploy` fail if it's ever unset. (CLERK_PRODUCTION.md.)
- **Clerk instance swap logs everyone out once** — expected on any dev→prod cutover (old sessions carry dev tokens). Not a bug.
- **Photo extraction** (`utils/pdfPhotos.js`): per-image decode needs a mandatory ~2s timeout (pdf.js never fires the callback for undecodable images → silent infinite hang); rank candidates by JPEG byte size not pixel area; reject blank/near-white images; bounded work (≤12 photos, ≤50 images, ≤25 pages).
- **Photo/video delete orphans the storage file** — delete only drops the id from the array; the underlying Convex file remains (deliberate, avoids breaking shared refs). A cleanup pass could reclaim these later.
- **Two Convex deployments** — always deploy to both (see WORKFLOW.md). `colorless-condor-502` is the one Vercel prod actually reads.
- **Open defects from the 2026-08-27 code audit (none fixed yet).** Found by audit, not by a user — record them here so they aren't rediscovered:
  - **`extractIM` has no auth check** (`convex/imExtractionAction.ts`). Every other action guards itself via `getCurrentUser`; this one doesn't, so any signed-in user — including a `client` or `blocked` role — can invoke it and burn Anthropic credits. Needs a valid `imExtractions` id, so exposure is limited, but it's a real hole.
  - **`scripts/check-authz.js` never scans actions.** Its regex is `/export const (\w+) = (query|mutation)\(\{/` — actions sit outside the build gate entirely, which is how `extractIM` slipped through. Widen to `query|mutation|action`.
  - **`formatCurrency` is forked five ways and the copies disagree.** `CompsTab`, `PropertyAssessmentTab`, `AddCompsModal`, `ProjectFeasibilityTab` and `DetailsTab` each define a local `fmt$` using `toFixed(2)` where `src/utils/format.js` uses `toFixed(1)`. The same sale renders **$3.8M** on one screen and **$3.75M** on another; zero renders as "—" vs "$0". Violates CLAUDE.md §4. User-visible in a valuation tool.
  - **WALE goes stale when tenancies are edited.** `calcWale()` runs once at IM-scan time (`ExtractionView.jsx`) and the result is stored on `properties.wales`. `updatePropertyTenants` patches `tenants` only, and `TenancyTab` — where lease expiries are actually edited — never recomputes it. The headline figure silently keeps its scan-time value.
  - **Data quality:** `postcode` is 0% populated on all 110 comps and no property has a `state` value — which is why suggested-comps widening to "same state" can't help them, and why Will's "surrounding suburbs" search is blocked. One comp has the literal string `"Upon Completion"` in a date field.
  - **`convex/dashboard.ts` caps `matches` at `.take(500)`** with no index on status — past 500 matches the "Properties in DD" stat silently under-counts. Fine at current scale, documented so it isn't a surprise later.
- **The codebase is not bloated — don't refactor for size.** The same audit measured 18,105 lines across 90 files, zero `.collect()` calls, zero dead files, route-level code splitting, ~180 KB gzipped initial load. Large files are compositions of small co-located components. The risks here are correctness and test coverage (14 tests), not structure.
- **Deploys are manual; the live site can lag the repo.** Latest prod deploy: **`oracle-crglxtes8`** (2026-09-04 — cap-rate suggestion from comps), both domains aliased.

## Outstanding — user actions (not code)

- **Rotate the leaked GitHub PAT** at github.com/settings/tokens (still compromised until done). After rotating, clear the stale keychain entry so git re-prompts.
- **Invoices** — Stage 2+3 ($12k) are user-side billing actions.
- **Bump GoDaddy DNS TTLs back up** — dropped to 600s/1800s during the launch for fast validation; raise now that DNS is stable. Minor.

## Pointers

- Production launch runbook: [CLERK_PRODUCTION.md](CLERK_PRODUCTION.md).
- **Backlog: `AG-ORACLE/BACKLOG.md` — rebuilt 2026-09-02** and verified against code; it is the ordered work list. Meeting sources: `AG-ORACLE/Google Meet Notes/` (26 Jun · 3 Jul · 31 Jul · 21 Aug) + `AG-ORACLE/meetings/` (May). Both outside the repo.
- Deep architecture history: `AG-ORACLE/oracle-architecture-summary.md` (dated 2026-05-22; this doc set supersedes it for current truth).
